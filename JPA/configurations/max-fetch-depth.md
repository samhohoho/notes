## max_fetch_depth

**Intro**

Specifies the
maximum depth of
nested outer join
fetching.

The number of
tables joined
depends on
this configuration property.

By default,
no limit is set.

Reasonable values are
from 1 to 5.

Disables
`JOIN` fetching
by setting
to 0.