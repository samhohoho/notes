## Database identity

### Object identity `(==)`

A notion
defined by
JVM.

Two references
are identical
if
point to
same
memory location.
___
### Object equality

Objects with
same state.
___
### Database identity

Objects
are identical
if
share the same
table
and
primary key value.