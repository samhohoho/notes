## JDBC (Java Database Connectivity)

JDBC API
a common interface
for communicating
with DB server.

Networking logic and
DB-specific
communication protocol
are hidden away behind
vendor-independent
JDBC API.

`java.sql.Driver`
main entry point to
JDBC API.