## `CharSequence` interface

**Intro**

Both `String` and `StringBuilder` implement this interface.

Contains methods that manage sequence of characters.

Work with other string-like classes.

If those operations suffice,
use `CharSequence` instances instead of strings.
___
**Usage**

Can get length, iterate, extract subsequences, and lexicographically.

Java 17 adds `isEmpty`.